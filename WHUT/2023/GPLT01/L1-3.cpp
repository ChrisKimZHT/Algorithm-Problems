#include <bits/stdc++.h>

using namespace std;

void solve()
{
    int val, off;
    cin >> val >> off;
    cout << fixed << setprecision(2) << 1.0 * val * off / 10 << endl;
}

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    solve();
    return 0;
}