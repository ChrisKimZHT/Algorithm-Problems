#include <bits/stdc++.h>

using namespace std;

void solve()
{
    int N, v;
    cin >> N >> v;
    cout << N / v << endl;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    solve();
    return 0;
}
